pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "GoFoodClone"
include ':app'
include ':auth:domain'
include ':auth:session'
include ':auth:register:persentation'
include ':auth:register:remote'
include ':auth:login:persentation'
include ':auth:login:remote'
